import Header from "@/components/layout/Header";
import type { FC } from "react";

const Landing: FC = () => {
  return (
    <section>
      <Header />
      Landing
    </section>
  );
};

export default Landing;
